source:
  type: udp
  bind_addr: "0.0.0.0"
  port: 5000

processors:
  - type: channel_filter
    channels:
      - "nEngine"
  - type: formula
    output: "omega"
    ast: "manifests/omega.json"
  - type: stats

sink:
  type: console