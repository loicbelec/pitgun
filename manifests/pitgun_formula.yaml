source:
  type: udp
  bind_addr: "0.0.0.0"
  port: 5000

processors:
  - type: channel_filter
    channels: ["nEngine", "throttle"]
  - type: formula
    output: "omega_engine"
    ast: "manifests/omega_engine.json"
  - type: stats

sink:
  type: console