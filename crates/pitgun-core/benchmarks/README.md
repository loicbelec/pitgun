# pitgun-core Benchmarks

Run the Criterion benchmarks for pitgun-core:

```bash
cargo bench -p pitgun-core
```

The current benchmark entrypoint lives in `crates/pitgun-core/benches/formula_processor_bench.rs`.
