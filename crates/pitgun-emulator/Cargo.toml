[package]
name = "pitgun-emulator"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
csv = "1"
socket2 = "0.5"
time = { version = "0.3", features = ["parsing", "formatting"] }