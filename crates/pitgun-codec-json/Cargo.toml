[package]
name = "pitgun-codec-json"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
pitgun-core = { path = "../pitgun-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
